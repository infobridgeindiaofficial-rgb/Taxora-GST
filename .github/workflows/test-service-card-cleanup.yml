name: Test service card cleanup

on:
  push:
    paths:
      - 'TAXORA GST.dc.html'
      - 'tests/service-card-cleanup-regression.mjs'
      - '.github/workflows/test-service-card-cleanup.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: node tests/service-card-cleanup-regression.mjs
